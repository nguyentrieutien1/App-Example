<!DOCTYPE html>
<html>
  <head>
    <title>RailsTraining</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <title><%= full_title yield :title %></title><%= csrf_meta_tags %><%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track": "reload" %><%= javascript_importmap_tags %>

    <%= javascript_importmap_tags %>
  </head>

  <body>
  <%= render "layouts/header" %>
  <div class="container">
    <%= yield %>
  </div>
  <div class="text-center">
    <%= render "layouts/footer" %>
  </div>
  </body>
</html>
